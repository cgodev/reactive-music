import Header from "./modules/shared/Header";
import Routing from "./routes/Routes";

const App = () => {
    return (
        <main>
            <Routing></Routing>
        </main>
    );
}

export default App;